const { ServiceBroker } = require('moleculer');

const broker = new servicebroker({
  nodeid: 'node-1',
  logger: console
});

broker.createService(require('./services/songs'));
broker.createService(require('./services/soundcloud'));

broker.start();
